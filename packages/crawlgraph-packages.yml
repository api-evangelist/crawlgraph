generated: '2026-09-02'
method: searched
source: https://registry.npmjs.org/crawlgraph-mcp
also_from:
  - https://github.com/pucilpet/crawlgraph-mcp
  - https://crawlgraph.com/docs/api
summary: >-
  One first-party published package: the MCP server on npm. There is NO conventional client SDK
  in any language. The docs teach the API with raw curl / Python requests / Node fetch snippets
  instead, which is a deliberate posture for a six-operation API but means an integrator gets no
  typed client. Registry probes for crawlgraph, crawlgraph-sdk, @crawlgraph/sdk (npm) and
  crawlgraph (PyPI) all returned 404.
package_count: 1
official_package_count: 1
sdk_count: 0

packages:
  - name: crawlgraph-mcp
    registry: npm
    url: https://www.npmjs.com/package/crawlgraph-mcp
    repository: https://github.com/pucilpet/crawlgraph-mcp
    language: TypeScript
    kind: mcp-server
    official: true
    license: MIT
    version: 0.2.2
    published: '2026-08-17'
    versions_published: 2
    all_versions: ['0.1.0', '0.2.2']
    previous_version: {version: 0.1.0, published: '2026-05-31'}
    registry_metadata: https://registry.npmjs.org/crawlgraph-mcp
    registry_modified: '2026-08-17'
    bin: crawlgraph-mcp
    install: npx -y crawlgraph-mcp
    runtime: node >=18
    dependencies: ['@modelcontextprotocol/sdk ^1.0.4', 'express ^4.21.2', 'zod ^3.23.8']
    description: >-
      MCP server for the CrawlGraph backlink-intelligence API. Four tools: backlinks,
      gap_analysis, gap_outreach_targets, releases.
    currency_note: >-
      CURRENCY GAP CLOSED. The 2026-08-13 pass recorded npm stuck on 0.1.0 (2026-05-31) while the
      GitHub master branch declared 0.2.2 — two minor versions and two months of drift, so that
      `npx -y crawlgraph-mcp` shipped something the repo no longer described. The operator
      published 0.2.2 to npm on 2026-08-17 and the gap is gone: registry latest, the master
      package.json, and the hosted server's own serverInfo.version (probed via MCP `initialize`
      at https://crawlgraph.com/mcp) all now read 0.2.2. Registry and source are in agreement,
      and the hosted endpoint is running the same version as the package. This is the first
      artifact in this repo to record a decay signal being repaired rather than worsening.
    tool_count: 4
    tools: [backlinks, gap_analysis, gap_outreach_targets, releases]
    tools_note: >-
      Four registerTool() calls in src/server.ts at master and four rows in the README's tool
      table. The API docs state the local package "documents five read-only tools" and name a
      fifth, backlink_changes — neither the published source nor the hosted tools/list carries it.
      See mcp/crawlgraph-tool-crosswalk.yml for the divergence.

registry_probes:
  - {registry: npm, package: crawlgraph-mcp, status: 200, found: true}
  - {registry: npm, package: crawlgraph, status: 404, found: false}
  - {registry: npm, package: crawlgraph-sdk, status: 404, found: false}
  - {registry: npm, package: '@crawlgraph/sdk', status: 404, found: false}
  - {registry: pypi, package: crawlgraph, status: 404, found: false}

not_packaged:
  - kind: sdk
    note: >-
      No first-party client SDK in any language. The API reference ships copy-paste curl, Python
      (requests) and Node (fetch) snippets for every operation instead.
  - kind: cli
    note: >-
      No installable CLI. The landing page renders a `crawl stripe.com --top 5` terminal
      animation as marketing illustration; no such binary is published or documented.

related_repositories:
  - name: crawlgraph-datasets
    url: https://github.com/pucilpet/crawlgraph-datasets
    kind: data
    license: CC-BY (stated on the site; no LICENSE file in the repo)
    description: >-
      Open datasets from the backlink-overlap studies (CSV + JSON, with a datapackage.json).
      Data, not an interface — not a package.
    pushed: '2026-07-06'
    rechecked: '2026-09-02'

distribution_channels:
  - {channel: npm, url: 'https://www.npmjs.com/package/crawlgraph-mcp'}
  - {channel: smithery, note: 'repo ships smithery.yaml with a stdio startCommand', url: 'https://github.com/pucilpet/crawlgraph-mcp/blob/master/smithery.yaml'}
  - {channel: docker, note: 'repo ships a Dockerfile and DEPLOY.md; no published image located'}

x-evidence:
  - {url: 'https://registry.npmjs.org/crawlgraph-mcp', status: 200, note: 'dist-tags.latest 0.2.2, time 2026-08-17T19:44:22Z'}
  - {url: 'https://raw.githubusercontent.com/pucilpet/crawlgraph-mcp/master/package.json', status: 200, note: 'version 0.2.2 — matches the registry'}
  - {url: 'https://data.jsdelivr.com/v1/packages/npm/crawlgraph-mcp', status: 200, note: 'tags.latest 0.2.2'}
  - {url: 'https://crawlgraph.com/mcp', status: 200, note: 'MCP initialize returns serverInfo {name: crawlgraph, version: 0.2.2}'}
  - {url: 'https://registry.npmjs.org/crawlgraph', status: 404}
  - {url: 'https://pypi.org/pypi/crawlgraph/json', status: 404}
